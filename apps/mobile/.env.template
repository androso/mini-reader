# A reachable HTTPS API origin for physical devices and internal builds.
# Physical iPhones need a phone-reachable HTTPS origin (ATS blocks cleartext
# except localhost). iOS Simulator can use http://localhost:3000 for local API
# development. Android emulators can use http://10.0.2.2:3000 for local-only
# development. Preview/production builds need HTTPS.
EXPO_PUBLIC_API_URL=https://reader.example.com
