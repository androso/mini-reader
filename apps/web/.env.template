# Local development calls the API on its own port and includes credentials.
# Leave this empty in production so Caddy serves API and web on one origin.
NEXT_PUBLIC_API_URL=http://localhost:3000
# Public browser client ID; normally the same value as API GOOGLE_CLIENT_ID.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
